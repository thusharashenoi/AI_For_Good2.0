# Set at build time for production (the deployed AdminApi base URL).
# In dev this is unused; Vite proxies /api -> http://127.0.0.1:8000.
VITE_API_BASE=https://YOUR_ADMIN_API_ID.execute-api.us-east-1.amazonaws.com/dev
