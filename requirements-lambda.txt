# Lambda-only deps (no scipy/sklearn/matplotlib/pyvis at runtime).
# Willingness scores are precomputed at seed time; allocation is greedy (no scipy).
pandas>=2.0
numpy>=1.24
networkx>=3.1
boto3>=1.34
mangum>=0.17
fastapi>=0.110
